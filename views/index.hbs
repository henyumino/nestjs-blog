<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>{{ title }}</title>
    <link rel="icon" href="{{ favicon }}">

    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        img[alt="Hacktoberfest"]{
            height: 100vh;
            width: 100%;
            object-fit: cover;
        }
    </style>
  </head>
  <body>
    <img src="{{ backdrop }}" alt="Hacktoberfest">
  </body>
</html>